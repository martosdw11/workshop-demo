/**
 * Placeholder root. Halaman sebenarnya dibangun pada epic berikutnya:
 * `(public)/login`, `(participant)/dashboard`, `(admin)`.
 */
export default function HomePage() {
  return (
    <main className="flex min-h-screen items-center justify-center p-gutter">
      <p className="text-body-md text-on-surface-variant">Learning Study AI — coming soon.</p>
    </main>
  );
}
